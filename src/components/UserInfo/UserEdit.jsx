import { useContext } from 'react'
import { EmployeeContext } from '../../store/employees-context'
import Input from '../Input'

export default function UserEdit() {

    const { employeesData, employeeEditing } = useContext(EmployeeContext)

    return (
        <div>
                        
        </div>
    )
}